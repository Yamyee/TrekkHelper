# 户外徒步App MapLibre 3D地形开发规范文档（AI可直接执行）

# 一、文档说明

## 1\.1 文档目的

本文档为户外徒步App iOS端，基于MapLibre SDK实现“类似奥维互动地图3D地形效果”的标准化开发规范，明确开发环境、核心需求、实现步骤、参数标准、验收标准，确保任何AI开发者可直接参照文档完成开发，无需额外沟通。

## 1\.2 适用范围

适用场景：户外徒步App iOS端地图模块，基于MapLibre SDK实现3D地形渲染、视角控制、图层叠加（OSM底图\+等高线\+卫星图），适配徒步用户核心需求（地形可视化、轨迹贴合）。

适用对象：所有AI开发者、技术开发人员，无需具备MapLibre专项经验，按文档步骤执行即可完成开发。

## 1\.3 核心目标

- 实现类似奥维互动地图的3D地形效果，支持立体地形渲染、视角俯仰/旋转/缩放

- 叠加OSM基础底图、OpenTopoMap等高线、谷歌卫星图，四层联动（底图\+等高线\+卫星图\+3D地形）

- 3D地形下，GPX徒步轨迹实现立体贴地渲染，与地形高度匹配

- 所有功能基于MapLibre SDK（免费开源），无密钥、无调用限制，国内坐标无偏移

- 适配iOS 13\.0及以上版本，兼容主流iPhone机型（iPhone 11及以上）

# 二、开发环境配置规范

## 2\.1 基础环境要求

|配置项|具体要求|说明（AI必看）|
|---|---|---|
|开发工具|Xcode 14\.0及以上|必须使用该版本及以上，确保兼容MapLibre SDK|
|iOS系统版本|最低适配iOS 13\.0|不低于该版本，覆盖95%以上徒步用户机型|
|编程语言|Swift 5\.0及以上|统一语言，避免语法兼容问题|
|依赖管理|CocoaPods 1\.11\.0及以上|用于集成MapLibre SDK，必须按规范配置Podfile|

## 2\.2 MapLibre SDK集成规范

步骤固定，AI直接复制执行，无需修改（仅替换项目名）：

1. 打开Xcode项目，创建/编辑Podfile，粘贴以下内容（替换“你的项目名”为实际项目名称）：
        `platform :ios, \&\#39;13\.0\&\#39;
use\_frameworks\!

target \&\#39;你的项目名\&\#39; do
  \# 地图核心SDK（免费开源，无密钥）
  pod \&\#39;MapLibre\&\#39;
end`

2. 打开终端，进入项目根目录，执行命令：`pod install \-\-repo\-update`，等待集成完成

3. 关闭Xcode，打开“项目名\.xcworkspace”文件，确认MapLibre SDK集成成功（导入MapLibre无报错）

## 2\.3 权限配置规范

必须配置定位权限，否则无法实现徒步定位和轨迹贴合，步骤如下：

1. 在Xcode项目中，找到“Info\.plist”文件

2. 添加两个权限键值对，缺一不可：
        

    - 键：NSLocationWhenInUseUsageDescription，值：“为了显示您的实时位置、绘制徒步轨迹，需要获取您的位置权限”

    - 键：NSLocationAlwaysAndWhenInUseUsageDescription，值：“为了后台记录徒步轨迹、无网状态下定位，需要获取您的始终位置权限”

# 三、核心功能开发规范（AI直接按步骤执行）

## 3\.1 基础地图初始化（必做，为3D地形打基础）

### 3\.1\.1 核心要求

初始化MapLibre地图，配置OSM基础底图，开启实时定位，设置默认视角，确保国内坐标无偏移（适配GCJ\-02）。

### 3\.1\.2 开发步骤

1. 创建MapViewController\.swift文件，继承UIViewController，遵循MLNMapViewDelegate协议

2. 在ViewController中声明mapView属性：`var mapView: MLNMapView\!`

3. 实现viewDidLoad方法，完成地图初始化，代码逻辑固定（无需修改，直接复制）：
        

    - 初始化地图：使用OSM开源样式，设置frame为屏幕全屏，添加到当前视图

    - 开启实时定位：显示用户位置，设置跟踪模式为“跟随”

    - 设置默认视角：纬度30\.123、经度103\.456（可适配国内徒步热门区域），缩放级别12，俯仰角30°

    - 开启手势：允许缩放、平移、俯仰、旋转（为3D操作做准备）

### 3\.1\.3 关键参数标准

|参数|取值|说明|
|---|---|---|
|地图样式URL|https://demotiles\.maplibre\.org/style\.json|OSM开源样式，无密钥，国内无偏移|
|默认经纬度|纬度30\.123，经度103\.456|国内徒步热门区域（川西/云南附近），可直接使用|
|默认缩放级别|12|兼顾路网细节和地形视野，适合徒步|
|默认俯仰角|30°|初步呈现3D效果，避免视角过平|
|手势设置|allowsZoom=true、allowsPan=true、allowsPitch=true、allowsRotate=true|开启所有3D操作手势，与奥维一致|

## 3\.2 3D地形开启（核心功能，类似奥维3D效果）

### 3\.2\.1 核心要求

使用免费全球高程DEM数据，开启3D地形渲染，设置地形夸张系数（与奥维常用效果一致），确保地形立体、自然，无卡顿。

### 3\.2\.2 开发步骤（在基础地图初始化完成后执行）

1. 添加高程DEM数据源（免费全球覆盖，精度适配徒步）：
        

    - 使用Mapzen（AWS）免费高程瓦片，无需申请密钥，直接使用指定URL

    - 创建MLNRasterTileSource，标识符固定为“terrain\-dem”，设置瓦片URL、缩放范围（2\-14级）

    - 将数据源添加到mapView\.style中

2. 开启3D地形渲染（一行代码即可）：
        

    - 创建MLNTerrain对象，关联上述DEM数据源

    - 地形夸张系数设置为2\.5（奥维常用夸张度，1=真实地形，2\.5=立体效果明显，不夸张）

    - 将terrain设置到mapView\.style中，完成3D地形开启

### 3\.2\.3 关键参数标准（不可修改，确保效果一致）

|参数|取值|说明|
|---|---|---|
|DEM瓦片URL|https://tile\.terrain500m\.com/tiles/\{z\}/\{x\}/\{y\}\.png|免费全球高程数据，精度500m，足够徒步使用|
|DEM数据源标识符|terrain\-dem|固定标识符，与后续3D地形配置关联，不可修改|
|DEM缩放范围|最小2级，最大14级|兼顾全局视野和局部地形细节，避免卡顿|
|地形夸张系数|2\.5|与奥维3D效果一致，既立体又不失真，徒步用户体验最佳|

## 3\.3 图层叠加（OSM\+等高线\+卫星图，与3D地形联动）

### 3\.3\.1 核心要求

实现四层图层叠加（OSM基础底图 → 等高线 → 卫星图 → 3D地形），图层可自由切换，切换后3D地形效果保持不变，等高线贴合3D地形。

### 3\.3\.2 开发步骤（按图层顺序添加，不可颠倒）

1. 1\. OSM基础底图（底层，提供路网、徒步小径）：
        

    - 瓦片URL：https://tile\.openstreetmap\.org/\{z\}/\{x\}/\{y\}\.png

    - 标识符：osm\-base，缩放范围1\-19级

    - 添加为MLNRasterStyleLayer，优先级最低（底层）

2. 2\. OpenTopoMap等高线（中层，徒步核心）：
        

    - 瓦片URL：https://tile\.opentopomap\.org/\{z\}/\{x\}/\{y\}\.png

    - 标识符：topo\-contour，缩放范围5\-17级

    - 添加为MLNRasterStyleLayer，优先级高于OSM底图，低于卫星图

    - 效果要求：等高线清晰，贴合3D地形，能清晰显示坡度、山峰

3. 3\. 谷歌卫星图（顶层，可选，用于探路）：
        

    - 瓦片URL：https://mt1\.google\.com/vt/lyrs=s\&amp;x=\{x\}\&amp;y=\{y\}\&amp;z=\{z\}

    - 标识符：satellite，缩放范围1\-20级

    - 添加为MLNRasterStyleLayer，优先级最高（顶层）

### 3\.3\.3 图层切换要求

需实现三个图层切换按钮（OSM底图、等高线\+OSM、卫星图），切换逻辑固定：

- 切换到OSM底图：隐藏等高线、卫星图，仅显示OSM底图\+3D地形

- 切换到等高线\+OSM：显示OSM底图\+等高线\+3D地形，隐藏卫星图

- 切换到卫星图：显示卫星图\+3D地形，隐藏OSM底图、等高线

- 切换过程无卡顿，3D地形状态保持不变（视角、夸张系数不变）

## 3\.4 3D地形下GPX轨迹绘制（徒步核心需求）

### 3\.4\.1 核心要求

GPX轨迹需贴合3D地形（随地形高低起伏），轨迹样式统一，支持实时绘制和导入绘制，与奥维轨迹显示效果一致。

### 3\.4\.2 开发规范

1. 轨迹样式（固定，不可修改）：
        

    - 颜色：纯红色（RGB：255,0,0），无透明效果

    - 线宽：4pt，清晰可见，不遮挡地形和等高线

    - 贴合方式：轨迹自动贴合3D地形，随地形高度变化，不悬浮、不穿透

2. 绘制逻辑（固定）：
        

    - 获取GPX文件解析后的经纬度坐标数组（CLLocationCoordinate2D类型）

    - 创建MLNPolyline对象，传入坐标数组和坐标数量

    - 通过mapView\.addAnnotation\(polyline\)添加轨迹

    - 实现MLNMapViewDelegate的轨迹样式代理方法，固定线宽和颜色

# 四、适配与优化规范（AI必做，确保体验）

## 4\.1 性能适配

- 卡顿优化：3D地形缩放、旋转时，帧率不低于30fps，无明显卡顿

- 内存优化：离线瓦片缓存不超过100MB，退出地图页面后自动释放内存

- 网络适配：无网状态下，已缓存的瓦片和3D地形可正常显示，不崩溃

## 4\.2 坐标适配

确保国内使用时，地图无偏移，配置固定：MapLibre默认适配WGS\-84坐标，需开启GCJ\-02偏移适配（无需额外代码，SDK已内置，确保使用本文档指定的OSM样式URL即可）。

## 4\.3 机型适配

兼容iPhone 11及以上机型，屏幕尺寸适配（刘海屏、全面屏），地图边缘不被遮挡，手势操作无死角。

# 五、验收标准（AI开发完成后，对照验收）

## 5\.1 功能验收

- 基础地图：能正常显示OSM底图，实时定位准确，视角可缩放、平移、俯仰、旋转

- 3D地形：开启后呈现立体效果，夸张系数适中，地形自然，无失真

- 图层叠加：OSM、等高线、卫星图可自由切换，切换后3D地形保持不变，等高线贴合地形

- 轨迹绘制：GPX轨迹贴合3D地形，样式符合规范，导入/实时绘制正常

## 5\.2 体验验收

- 无卡顿：所有操作（切换图层、旋转视角、绘制轨迹）帧率≥30fps

- 无偏移：国内区域（如川西、云南）地图坐标与实际位置一致，无偏移

- 无崩溃：连续操作30分钟（切换图层、缩放视角、导入轨迹），无闪退、无报错

# 六、注意事项（AI必须遵守）

1. 所有数据源（OSM、OpenTopoMap、DEM、谷歌卫星图）均为免费开源/免费可用，无需申请密钥，直接使用本文档指定的URL，禁止修改URL

2. 参数不可随意修改（如地形夸张系数、轨迹样式、缩放范围），确保与奥维效果一致

3. 开发完成后，必须在iPhone 11、iPhone 13、iPhone 15三种机型上测试，确保适配

4. 禁止添加额外冗余代码，仅实现本文档指定的功能，避免影响性能

5. 国内使用无需额外配置坐标偏移，本文档指定的OSM样式已适配GCJ\-02

> （注：文档部分内容可能由 AI 生成）
